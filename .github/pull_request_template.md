## 关联

Closes #

## 变更

## 离线验证

- [ ] `npm run typecheck`
- [ ] `npm test`，新增或修改的测试：
- [ ] 黄金文件（`tests/golden/`）如有更新，已在此说明变化：

## 在线验证

带 `needs-live-check` 标签的 PR 必填；否则删掉本节。

- 命令：
- 对比 id / 会话 id：
- 汇总表片段（不要贴 key，不要贴 `.env`）：

## 文档

- [ ] README（改了 CLI 行为才需要）
- [ ] AGENTS.md（改了 CLI 行为、落盘格式或流程才需要）
- [ ] `CHANGELOG.md` 的 Unreleased 加了一行
